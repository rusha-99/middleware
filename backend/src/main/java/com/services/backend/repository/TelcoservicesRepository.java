package com.services.backend.repository;

import com.services.backend.entity.Telcoservices;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TelcoservicesRepository extends JpaRepository<Telcoservices,Integer> {
}
